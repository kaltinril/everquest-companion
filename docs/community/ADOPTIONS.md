# Third-party pull requests: the adoption ledger

Every upstream PR that was looked at gets a row, adopted or not (RULES.md, rule 12). The upstream
head commit is the one vetted; if the PR moves, it is vetted again before the `community/` branch
follows it. A deferred PR is re-evaluated from its row.

Upstream is `jmoyers/everquest-companion`. `gh pr list --repo jmoyers/everquest-companion` lists
what is open; `git fetch upstream` refreshes the `upstream/pr/N` refs.

## Adopted

| PR | Title | Author | Upstream head | Vetted | Our branch | What the vetting found |
|---|---|---|---|---|---|---|
| [#16](https://github.com/jmoyers/everquest-companion/pull/16) | ci: bump actions/setup-node from 4.4.0 to 7.0.0 | dependabot[bot] | `1b757c5b` | 2026-09-15 | `community/pr-16-setup-node` | Diff is exactly two workflow files, SHA only. The creator's `# v4 (v4.4.0)` trailing comments were not rewritten, so the new SHA was checked against `actions/setup-node` tag v7.0.0 via the GitHub API: matches. Workflows do not run on this fork; no runtime effect. Gate: typecheck, lint, suite unchanged. |
| [#59](https://github.com/jmoyers/everquest-companion/pull/59) | ci: bump actions/cache from 4.3.0 to 6.1.0 | dependabot[bot] | `60185e25` | 2026-09-15 | `community/pr-59-actions-cache` | One line in `build.yml`. SHA matches tag v6.1.0. Stale comment as above. |
| [#65](https://github.com/jmoyers/everquest-companion/pull/65) | ci: bump actions/deploy-pages from 4.0.5 to 5.0.1 | dependabot[bot] | `ef34f314` | 2026-09-15 | `community/pr-65-deploy-pages` | One line in `pages.yml`. SHA matches tag v5.0.1. Stale comment as above. |
| [#70](https://github.com/jmoyers/everquest-companion/pull/70) | deps: bump the npm-minor-and-patch group across 1 directory with 14 updates | dependabot[bot] | `d405a937` | 2026-09-15 | `community/pr-70-npm-minor-patch` (+2 commits) | Diff is exactly `package.json` and the lockfile; the 14 versions match the title; 107 added `resolved` URLs, all `registry.npmjs.org`, all with `integrity`. Clean against `main`. **Two bumps fail the gate and are pinned back on our branch:** (1) `typescript-eslint` 8.70 tightened `no-meaningless-void-operator` and `no-inferrable-types`; five of the seven new lint errors are in the creator's own unchanged files (`storeFile.ts`, `logEventKinds.ts`, `speechText.ts`, the `void _exhaustive` idiom), so the bump turns his lint red on an unmodified `main`. (2) `onnxruntime-node` 1.29 moves its binaries from `bin/napi-v3` to `bin/napi-v6`; `electron-builder.yml`'s platform excludes and `src/main/speech/vcRuntime.ts` both name the old path, so the installer grew from 136 MB to 208 MB (283 MB of foreign-platform binaries inside) and the voice-alert runtime check would not find the binding at all. Both need his files changed. With the pins: typecheck, lint and suite at the known reds, `npm run dist` builds on electron 43.7.0. |
| [#23](https://github.com/jmoyers/everquest-companion/pull/23) | Tests: make the bundledImages absolute-root check platform-independent | dbspringer | `00d0459f` | 2026-09-15 | `community/pr-23-bundled-images-win32` | One assertion in one test: `win32.isAbsolute` instead of the host's, because the fixture is win32-shaped. No runtime effect. Read whole; no rule 7 items; non-ASCII is one em dash. Gate on the branch alone: typecheck, lint green; one perf-budget test (`F13: FAST`) red under three concurrent gate runs and green twice alone at a third of its budget, a timing flake, not the PR's. |
| [#63](https://github.com/jmoyers/everquest-companion/pull/63) | Bonus experience lines are experience: the parser claims them | Amerzel | `8ae94e5f` | 2026-09-15 | `community/pr-63-bonus-exp` | Widens the engine's experience regex with a non-capturing `(?: \(with a bonus\))?` before the bang; group indices unchanged, unit test for both arms plus a quoted-chat refusal. Claim checked against the owner's log: 1,528 lines of exactly that shape (Sep 3 to Sep 7, a bonus weekend), all four arms present, none matched before. Hand-widens `factoring-baseline.json` for `world.rs` (445 to 448) because rustfmt wraps the longer regex; the register otherwise refuses growth, so that is the author's integrator call, accepted. Gate on the branch alone: typecheck, lint, suite 4198/0, `cargo test -p eqlog` 47/0, ratchet green. Same regex as #68's parser hunk; this spelling wins because the creator's CI runs `cargo fmt --check`. |
| [#68](https://github.com/jmoyers/everquest-companion/pull/68) | fix(bosses): "This week" stops tracking raid kills after EQ Legends changed its XP line | brianmontanaweb | `a88e400d` | 2026-09-15 | `community/pr-68-week-clears` (+1 commit) | Two things under one title. (1) The same regex widening as #63; on merge into `main_community` the conflict resolves to #63's spelling (rerere holds it). (2) A new week-view feature: a manual "base tier cleared" mark, gated on a credited open-world or unknown-tier kill inside the current lockout window, stored under `eq.bosses.weekClears.<name>_<server>`, greening only tier 0 and only when no real lock exists; 13 unit tests. **Title overstates:** the owner's log shows the bonus shape ran Sep 3 to Sep 7 only, with plain lines before and after; the line did not change, a bonus weekend happened. **Rule 6 hit:** the PR's `docs/plans/boss-lockout-credit-and-manual-clear.md` carries text addressed to AI agents ("for agentic workers: required sub-skill ...", Claude co-author trailers in commit templates). Owner ruling 2026-09-15: adopt the code, drop the document; the one commit on our branch removes it. No dependency, script, network or env change; no invisible unicode. Known soft spots, not blockers: a toggle inside the clock interval right after a Tuesday reset can delete a last-week mark instead of re-stamping it (self-heals next tick); `BossView` sits at 99 of 100 function lines. Gate on the branch alone: typecheck, lint, suite 4211/0, `cargo test -p eqlog` green, ratchet green. |
| [#47](https://github.com/jmoyers/everquest-companion/pull/47) | Bosses: hide mobs from Raid Targets - a per-target flag, the toolbar's peek, and an honest denominator (fixes #32) | johnsideserf | `e3ebc5da` | 2026-09-15 | `community/pr-47-hide-raid-targets` (+2 commits) | A fourth localStorage set (`eq.bosses.hidden`) through the existing `useQuestFlags` factory; the three existing sets and their keys are untouched, no migration. Hidden targets leave the roster and the tally denominator; the toolbar's peek (unpersisted `useState`) shows them dimmed with a restore button; kills still record on the full target list, so hiding is display-only. Verified 32 targets, 32 unique lowercased names. **The branch was red on `npm run lint` alone:** two `max-depth` errors in the PR's own new e2e spec (`tests/e2e/bosses-hide.e2e.mts`); the first commit on our branch lifts the second launch into its own function, assertions unchanged. **And red again once merged:** its unit test imported `tests/bossHistories.mts`, which the creator retired in JOS-499 with the TypeScript kills module (the PR is based 500 commits back); the second commit on our branch states the same two-boss history by hand through the surviving `allStatuses`, six assertions untouched. Soft spot for the creator: the toolbar count reads the raw store size, so a hide whose target is later renamed keeps "Hidden (1)" alive with nothing to clear it. Conflicts with #68 in `BossView.tsx` and `BossSections.tsx` (independent props on the same signatures); merged after #68, every region the union, rerere holds it. Gate on the branch alone after the fix: lint green, suite 5237/0. |

## Deferred

| PR | Title | Author | Upstream head | Looked at | Why deferred | Re-evaluate when |
|---|---|---|---|---|---|---|
| [#18](https://github.com/jmoyers/everquest-companion/pull/18) | deps(dev): bump vite from 7.3.6 to 8.2.1 | dependabot[bot] | `upstream/pr/18` | 2026-09-15 | `electron-vite` 5.0.0 declares `vite` peer `^5 \|\| ^6 \|\| ^7`; vite 8 is outside it. | electron-vite publishes a release whose peer range includes vite 8, and the creator bumps it. |
| [#19](https://github.com/jmoyers/everquest-companion/pull/19) | deps(dev): bump @eslint/js from 9.39.5 to 10.0.1 | dependabot[bot] | `upstream/pr/19` | 2026-09-15 | `@eslint/js` 10 has peer `eslint ^10.0.0`; the repo is on eslint 9.39.5. The bump alone is an unsatisfiable peer. | An eslint 10 bump appears (dependabot or the creator), and its config migration is his. |
| [#20](https://github.com/jmoyers/everquest-companion/pull/20) | deps: bump @mui/x-data-grid from 7.29.13 to 9.11.0 | dependabot[bot] | `cda96c4c` | 2026-09-15 | Tried: merges clean, but `npm ci` refuses. 9.x has peer `@mui/material ^7.3 \|\| ^9`; the repo is on 6.5.0. Note the package is imported by nothing under `src/` on any branch: it is an unused dependency. | The creator moves to `@mui/material` 7, or drops the dependency. |
| [#21](https://github.com/jmoyers/everquest-companion/pull/21) | deps(dev): bump eslint-plugin-react-hooks from 5.2.0 to 7.1.1 | dependabot[bot] | `3e8a4a35` | 2026-09-15 | Tried: merges clean, installs, but `npm run lint` exits 2 before linting. 7.x changed the shape of `configs['recommended-latest']`, which `eslint.config.mjs` spreads into the flat config. That file is the creator's. | The creator updates the react-hooks block in `eslint.config.mjs`, or a later plugin release restores the flat-config export. |
| [#51](https://github.com/jmoyers/everquest-companion/pull/51) | EQ discovery: the Daybreak launcher states the install dir as a FILE in it, and a machine can have three EverQuests | wangel | `90ab31a9` | 2026-09-15 | Read whole; no rule 7 items (registry reads add `UninstallString` and `DisplayIcon` on the same eight roots, no process spawned, no network). Gate green on the branch alone. **Not applied, owner ruling 2026-09-15, two reasons.** (1) A regression: `legendsFirst` is applied to the whole extra-candidates tier, which `config.ts` builds as env candidates plus registry candidates, so a registry path naming "EverQuest Legends" now outranks an explicit `EQ_INSTALL_DIR` that does not. The e2e fixture harness hands its staged install over exactly that way, so on any machine whose registry names a Legends install (the owner's included) e2e would tail the real game. Reproduced in isolation. (2) A retail-only machine resolved to nothing before and resolves to retail EverQuest now; by the PR's own argument that is the wrong game found, and it is the creator's design call. | The author partitions only the registry slice (`[...envCandidates(), ...legendsFirst(registryInstallCandidates())]`) and the creator rules on retail-only resolution. |
| [#48](https://github.com/jmoyers/everquest-companion/pull/48) | E2e portability + PR CI: two specs stop betting on a real install, and pull requests get the verify gate | johnsideserf | `07f57d84` | 2026-09-15 | Read whole; no invisible unicode. Adds a `pull_request` job to `.github/workflows/build.yml` and restages two e2e specs on the committed fixture. Rule 6: a workflow change is never adopted as-is. It also conflicts with `main` in both specs, which moved upstream since, and workflows do not run on this fork. **Blocked, owner ruling 2026-09-15: no changes to `.github/workflows` on the fork at this time.** | The owner lifts the workflow ruling, and the creator has taken or refused the PR CI job on his side. |

## Already in `main`

Open on GitHub, but the creator took the commits by rebase and never closed the PR. No
`community/` branch: the code is on every build through `main`.

| PR | Title | Author | Upstream head | Looked at | What the vetting found |
|---|---|---|---|---|---|
| [#33](https://github.com/jmoyers/everquest-companion/pull/33) | Sky: a held quest reward marks its quest turned in (fixes #27) | johnsideserf | `178ecc71` | 2026-09-15 | Both PR commits are patch-identical (`git patch-id --stable`) to `f068e8b3` and `9ad1c15f` on `main`, same author, 2026-08-16. The creator then extended the mechanism in `d28d5b3f` (JOS-429: `achievementInference.ts`, `withDerivedCompletion`, a `completionEvidence` ladder replacing `rewardInferred`). A `community/pr-33` branch would conflict in all seven files and regress that extension. Read whole anyway: no rule 7 items. Two soft spots the creator's version inherits: the export fallback to another character's inventory can mark a fresh character's quests turned in with undo disabled, and the untradeable regex is not word-bounded (matches all 93 flagged rewards today, fragile on rescrape). |

## Not yet looked at

Open upstream PRs from other contributors still unread as of 2026-09-15 evening, in the order
they should probably be looked at: independent features first, then the ones that overlap our own
branches and need a purpose-level comparison before anything else.

| PR | Title | Author | Overlaps |
|---|---|---|---|
| [#67](https://github.com/jmoyers/everquest-companion/pull/67) | feat(combat): show my character name instead of "You" in the DPS meter | brianmontanaweb | none known |
| [#54](https://github.com/jmoyers/everquest-companion/pull/54) | feat: add macOS build and packaging support | regnare | `electron-builder.yml` (test-neutering edits it too) |
| [#38](https://github.com/jmoyers/everquest-companion/pull/38) | Gear Tab Enhancement: Gear Planner Feature | JKane10 | `gear-tab-improvements`, `gear-progression-plan`, `exaltation-clarity` |
| [#12](https://github.com/jmoyers/everquest-companion/pull/12) | Sky: make quest progression easier to scan and filter | Evan-Coleman | Sky quest list |
| [#46](https://github.com/jmoyers/everquest-companion/pull/46) | AI assistant: ask about this character over the game | DaveVoyles | new network surface; RULES.md rule 15 |
| [#42](https://github.com/jmoyers/everquest-companion/pull/42) | feat(chat): capture and save player chat | Jibblits | log parser, storage |

Every dependabot PR open on 2026-09-15 is in Adopted or Deferred above. A `community/` branch
exists only for adopted PRs; a deferred one is re-tried from its `upstream/pr/N` ref.
